package com.claustrum.ui.theme

import androidx.compose.ui.graphics.Color

// Tesla / Optimus palette — see docs/design/ui (near-monochrome graphite/white,
// single red accent, steel for L0/info, amber for attention).

// Dark (signature)
val Graphite = Color(0xFF16181D)
val SurfaceDark = Color(0xFF1E2229)
val Surface2Dark = Color(0xFF262B33)
val LineDark = Color(0xFF30353E)
val InkDark = Color(0xFFF5F6F8)
val MutedDark = Color(0xFF9AA1AC)
val FaintDark = Color(0xFF6A7280)

// Light
val Paper = Color(0xFFFFFFFF)
val SurfaceLight = Color(0xFFF5F6F7)
val Surface2Light = Color(0xFFEDEEF0)
val LineLight = Color(0xFFE2E4E8)
val InkLight = Color(0xFF16181D)
val MutedLight = Color(0xFF5B636E)
val FaintLight = Color(0xFF9AA1AC)

// Accent + semantic (theme-independent)
val TeslaRed = Color(0xFFE82127)   // armed / critical / CTA
val OnAccent = Color(0xFFFFFFFF)
val Steel = Color(0xFF63A0C4)      // L0 watch / info / positive
val Amber = Color(0xFFE5A400)      // attention
